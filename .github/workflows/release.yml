name: Create Release and Package Module

on:
  push:
    tags:
      - 'v*'
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: release-${{ github.event.release.tag_name || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get Module Version & Determine Tag
        id: release-meta
        shell: bash
        run: |
          VERSION="$(jq -r .version module.json)"
          echo "version_number=$VERSION" >> $GITHUB_OUTPUT

          TAG="${{ github.event.release.tag_name || github.ref_name }}"
          if [[ -z "$TAG" || "$TAG" == "main" || "$TAG" == "master" || "$TAG" == "refs/heads/"* ]]; then
            TAG="v${VERSION}"
          fi
          echo "tag_name=$TAG" >> $GITHUB_OUTPUT

      - name: Update Manifest and Download URLs
        shell: bash
        run: |
          TAG="${{ steps.release-meta.outputs.tag_name }}"
          jq --arg manifest "https://github.com/${{ github.repository }}/releases/latest/download/module.json" \
             --arg download "https://github.com/${{ github.repository }}/releases/download/${TAG}/module.zip" \
             '.manifest = $manifest | .download = $download' module.json > module.json.tmp && mv module.json.tmp module.json

      - name: Check for Pre-release
        id: prerelease-check
        shell: bash
        run: |
          if [[ "${{ steps.release-meta.outputs.version_number }}" == *-* ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          fi

      - name: Create ZIP package
        run: |
          zip -r ./module.zip . -x ".git/*" ".github/*" "dist/*" ".gitignore"

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release-meta.outputs.tag_name }}
          name: Version ${{ steps.release-meta.outputs.version_number }}
          prerelease: ${{ steps.prerelease-check.outputs.is_prerelease }}
          overwrite: true
          files: |
            ./module.zip
            ./module.json